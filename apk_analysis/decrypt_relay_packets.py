# decrypt_relay_packets.py
# Focus on the 111-byte app->relay packets with 00 0d header

import struct

P2P_PE_TABLE = bytes([
    ,
])

def derive_key(psk):
    k0 = sum(psk) & 0xFF
    k1 = (-k0) & 0xFF
    k2 = sum(b // 3 for b in psk) & 0xFF
    k3 = 0
    for b in psk:
        k3 ^= b
    return bytes([k0, k1, k2, k3])

def decrypt(key4, data):
    tables = []
    for k in range(4):
        t = bytes(P2P_PE_TABLE[(key4[k] + prev) & 0xFF] for prev in range(256))
        tables.append(t)
    out = bytearray()
    prev = 0
    for c in data:
        out.append(c ^ tables[prev & 3][prev])
        prev = c
    return bytes(out)

def safe_text(data):
    return ''.join(chr(b) if 32 <= b < 127 else '.' for b in data)

def parse_pcap(path):
    with open(path, 'rb') as f:
        data = f.read()
    magic = data[:4]
    endian = '>' if magic == b'\xa1\xb2\xc3\xd4' else '<'
    offset = 24
    pkts = []
    while offset < len(data):
        if offset + 16 > len(data):
            break
        ts_sec, ts_usec, incl_len, orig_len = struct.unpack_from(f'{endian}IIII', data, offset)
        offset += 16
        if offset + incl_len > len(data):
            break
        pkts.append(data[offset:offset+incl_len])
        offset += incl_len
    return pkts

KEY_CAMERA = derive_key(b"camera")

CAPTURES = [
    (r'E:\open_camera\apk_analysis\capture1_new.pcap', 1),
    (r'E:\open_camera\apk_analysis\capture2_new.pcap', 276),
    (r'E:\open_camera\apk_analysis\capture3_new.pcap', 276),
]

# Known plaintext candidates
PLAINTEXTS = [
    b'{"pro":"check_user","cmd":100,"devmac":"0000","user":"admin","pwd":"1234"}',
    b'{"pro":"check_user","cmd":100,"devmac":"0000","user":"admin","pwd":"1234"}\x00',
    b'{"pro":"stream","cmd":111,"video":1,"camsmode":0,"user":"admin","pwd":"1234"}',
    b'{"pro":"dev_control","cmd":102,"user":"admin","pwd":"1234","heart":1}',
]

print("Decrypting 111-byte app->relay packets with PSK='camera'...")
print("=" * 80)

for cap_path, link_type in CAPTURES:
    print(f"\n{cap_path}:")
    pkts = parse_pcap(cap_path)
    
    for i, pkt in enumerate(pkts):
        if link_type == 1:  # Ethernet
            if len(pkt) < 14 or struct.unpack_from('>H', pkt, 12)[0] != 0x0800:
                continue
            ip_off = 14
        else:  # SLL2
            if len(pkt) < 20 or struct.unpack_from('>H', pkt, 10)[0] != 0x0800:
                continue
            ip_off = 20
        
        if len(pkt) < ip_off + 20:
            continue
        
        ip_len = (pkt[ip_off] & 0x0F) * 4
        proto = pkt[ip_off + 9]
        dst_ip = pkt[ip_off + 16:ip_off + 20]
        
        if proto != 17 or not (dst_ip[0] == 36 and dst_ip[1] == 137):
            continue
        
        udp_off = ip_off + ip_len
        payload = pkt[udp_off + 8:]
        
        if len(payload) != 111:
            continue
        
        if payload[:2] != b'\x00\x0d':
            continue
        
        # Try various header lengths
        for header_len in [13, 14, 15, 16, 17, 18, 19, 20]:
            if header_len >= len(payload):
                continue
            ciphertext = payload[header_len:]
            decrypted = decrypt(KEY_CAMERA, ciphertext)
            text = safe_text(decrypted)
            
            score = 0
            for marker in [b'"pro"', b'"cmd"', b'check_user', b'stream', b'admin', b'1234']:
                if marker in decrypted:
                    score += 1
            
            if score >= 2:
                print(f"  *** PKT {i}: header={header_len}, score={score}")
                print(f"      Decrypted: {text[:100]}")
                print(f"      Hex: {decrypted[:50].hex()}")
            elif b'{' in decrypted and b'}' in decrypted:
                print(f"  ? PKT {i}: header={header_len}, possible JSON")
                print(f"    Decrypted: {text[:100]}")

print("\nDone.")
