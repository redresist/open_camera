#!/usr/bin/env python3
"""Test alternative algorithm: ks[i] = P2P_PE_TABLE[(key4[c[i] & 3] + c[i]) & 0xFF]"""
import sys
import struct

sys.path.insert(0, r'E:\open_camera\apk_analysis')
from parse_pcap import parse_pcap

P2P_PE_TABLE = bytes([])

rev_table = {}
for i, v in enumerate(P2P_PE_TABLE):
    if v not in rev_table:
        rev_table[v] = []
    rev_table[v].append(i)

pkts = parse_pcap(r'E:\open_camera\apk_analysis\capture1_new.pcap')
pkt23 = pkts[23][2]
ip_start = 14
ip_header_len = (pkt23[ip_start] & 0x0F) * 4
udp_start = ip_start + ip_header_len
payload = pkt23[udp_start+8:]
c16 = payload[16:]

base_json = b'{"pro":"check_user","cmd":100,"devmac":"0000","user":"admin","pwd":"1234"}'
pt16 = (base_json + b'\x00' * (96 - len(base_json)))[:95]

ks = bytes(c ^ p for c, p in zip(c16, pt16))

print("Testing alternative algorithm:")
print("ks[i] = P2P_PE_TABLE[(key4[c[i] & 3] + c[i]) & 0xFF]")

# For each position i, we need P2P_PE_TABLE[(key4[c[i]&3] + c[i]) & 0xFF] = ks[i]
# This gives us constraints on key4[c[i]&3]

key_constraints = {0: set(range(256)), 1: set(range(256)), 2: set(range(256)), 3: set(range(256))}

for i in range(min(95, len(ks))):
    c = c16[i]
    needed_key_idx = c & 3
    target_ks = ks[i]
    valid_k_values = set()
    for idx in rev_table.get(target_ks, []):
        k = (idx - c) & 0xFF
        valid_k_values.add(k)
    
    key_constraints[needed_key_idx] &= valid_k_values
    
    if not key_constraints[needed_key_idx]:
        print(f"No valid key values for key4[{needed_key_idx}] at position {i}")
        break

print("\nKey constraints after all positions:")
for idx in range(4):
    print(f"  key4[{idx}]: {len(key_constraints[idx])} valid values")
    if len(key_constraints[idx]) <= 20:
        print(f"    Values: {sorted(key_constraints[idx])}")

# Check if all key positions have at least one valid value
if all(len(key_constraints[i]) > 0 for i in range(4)):
    print("\nAll positions have valid values! Testing combinations...")
    count = 0
    for k0 in sorted(key_constraints[0]):
        for k1 in sorted(key_constraints[1]):
            for k2 in sorted(key_constraints[2]):
                for k3 in sorted(key_constraints[3]):
                    key4 = bytes([k0, k1, k2, k3])
                    # Test decryption with the alternative algorithm
                    out = bytearray()
                    for c in c16:
                        k = key4[c & 3]
                        idx = (k + c) & 0xFF
                        ks_byte = P2P_PE_TABLE[idx]
                        out.append(c ^ ks_byte)
                    plaintext = bytes(out)
                    
                    # Check if it matches the expected plaintext
                    if plaintext[:len(pt16)] == pt16[:len(pt16)]:
                        print(f"  PERFECT MATCH: key4={key4.hex()}")
                        count += 1
    
    print(f"Total perfect matches: {count}")
else:
    print("\nSome key positions have no valid values. Algorithm is incorrect.")
