#!/usr/bin/env python3
"""Test keystream feedback (OFB-like) algorithm."""
import sys

sys.path.insert(0, r'E:\open_camera\apk_analysis')
from parse_pcap import parse_pcap

P2P_PE_TABLE = bytes([])

rev_table = {}
for i, v in enumerate(P2P_PE_TABLE):
    if v not in rev_table:
        rev_table[v] = []
    rev_table[v].append(i)

pkts = parse_pcap(r'E:\open_camera\apk_analysis\capture1_new.pcap')
pkt23 = pkts[23][2]
ip_start = 14
ip_header_len = (pkt23[ip_start] & 0x0F) * 4
udp_start = ip_start + ip_header_len
payload = pkt23[udp_start+8:]
c16 = payload[16:]

base_json = b'{"pro":"check_user","cmd":100,"devmac":"0000","user":"admin","pwd":"1234"}'
pt16 = (base_json + b'\x00' * (96 - len(base_json)))[:95]

ks = bytes(c ^ p for c, p in zip(c16, pt16))

print("Testing OFB-like algorithm (prev = keystream byte):")

# Check if any key4[0] satisfies first few constraints
# i=0: P2P_PE_TABLE[key4[0]] = ks[0] = 0x34
# i=1: P2P_PE_TABLE[(key4[ks[0]&3] + ks[0]) & 0xFF] = ks[1]
#       ks[0] = 0x34, ks[0]&3 = 0
#       P2P_PE_TABLE[(key4[0] + 0x34) & 0xFF] = 0xd0
# i=2: P2P_PE_TABLE[(key4[ks[1]&3] + ks[1]) & 0xFF] = ks[2]
#       ks[1] = 0xd0, ks[1]&3 = 0
#       P2P_PE_TABLE[(key4[0] + 0xd0) & 0xFF] = 0xff

valid_k0 = set()
for k0 in rev_table.get(ks[0], []):
    idx1 = (k0 + ks[0]) & 0xFF
    if P2P_PE_TABLE[idx1] == ks[1]:
        idx2 = (k0 + ks[1]) & 0xFF
        if P2P_PE_TABLE[idx2] == ks[2]:
            valid_k0.add(k0)

print(f"Valid key4[0] values: {len(valid_k0)}")
if valid_k0:
    print(f"  Values: {sorted(valid_k0)}")
    
    # Now check further constraints on key4[0]
    # i=3: ks[2]=0xff, ks[2]&3=3. Need key4[3] constrained.
    # i=4: ks[3]=0x91, ks[3]&3=1. Need key4[1] constrained.
    # But first let's see how far we can get with just key4[0]
    
    # Actually, in OFB, ks[i] & 3 determines which key byte is used for ks[i+1]
    # We need to track the full key4[0..3]
    
    # Build key4 constraints step by step
    candidates = []
    for k0 in sorted(valid_k0):
        candidates.append(([k0], 3, ks[2]))  # (key_bytes, match_count, last_ks)
    
    for i in range(3, min(31, len(ks))):
        new_candidates = []
        target_ks = ks[i]
        for key_bytes, count, last_ks in candidates:
            needed_idx = last_ks & 3
            if needed_idx < len(key_bytes):
                k = key_bytes[needed_idx]
                idx = (k + last_ks) & 0xFF
                if P2P_PE_TABLE[idx] == target_ks:
                    new_candidates.append((key_bytes, count + 1, target_ks))
            else:
                for idx in rev_table.get(target_ks, []):
                    k = (idx - last_ks) & 0xFF
                    new_key = key_bytes + [k]
                    new_candidates.append((new_key, count + 1, target_ks))
        
        candidates = new_candidates
        if not candidates:
            print(f"No candidates at position {i}")
            break
        
        max_count = max(c[1] for c in candidates)
        candidates = [c for c in candidates if c[1] == max_count]
        if len(candidates) > 10000:
            candidates = candidates[:10000]
        
        if i % 5 == 0:
            print(f"Position {i}: {len(candidates)} candidates, max_match={max_count}")
    
    if candidates:
        print(f"\nFound {len(candidates)} key candidates:")
        for key_bytes, count, _ in candidates[:10]:
            print(f"  key4 = {bytes(key_bytes).hex()}, matched {count} bytes")
else:
    print("No valid key4[0] found. Algorithm is incorrect.")
